{
  "name": "@mias/relay",
  "version": "0.1.0",
  "private": true,
  "description": "Mias Bridge rendezvous relay — a zero-knowledge byte forwarder. Routes on rendezvous_id + labeled channel, requires a slot token to reclaim, expires idle rooms, rate-limits. Never sees plaintext.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": { "mias-relay": "./src/bin.ts" },
  "scripts": {
    "start": "node src/bin.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/ws": "^8.5.12",
    "typescript": "^7.0.2"
  }
}
