<?xml version="1.0" encoding="utf-8"?>
<!-- Mias adaptive icon — background layer.
     Deep navy vertical gradient with a faint cyan aura behind the sword:
     dark enough for the steel to gleam, tied to the app's navy+cyan identity. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Base: midnight gradient, lighter toward the top where the blade points. -->
    <path android:pathData="M0,0 L108,0 L108,108 L0,108 Z">
        <aapt:attr name="android:fillColor">
            <gradient
                android:type="linear"
                android:startX="54" android:startY="0"
                android:endX="54" android:endY="108"
                android:startColor="#16203A"
                android:endColor="#060912" />
        </aapt:attr>
    </path>

    <!-- Cyan aura: soft radial glow centered slightly above middle, where the
         blade and gem sit. Very low alpha — atmosphere, not a spotlight. -->
    <path android:pathData="M54,50m-50,0a50,50 0,1 1,100 0a50,50 0,1 1,-100 0">
        <aapt:attr name="android:fillColor">
            <gradient
                android:type="radial"
                android:centerX="54" android:centerY="50"
                android:gradientRadius="50"
                android:startColor="#2400E5FF"
                android:endColor="#0000E5FF" />
        </aapt:attr>
    </path>
</vector>
